packages:
  - "apps/*"
  - "packages/*"

# Các gói được phép chạy script cài đặt (pnpm chặn mặc định để an toàn)
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
